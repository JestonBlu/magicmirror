include:
  - includes/${MM_INIT}.yaml
  - includes/${MM_MMPM}.yaml

services:
  magicmirror:
    container_name: mm
    ports:
      - "8080:8080"
    restart: unless-stopped
    extends:
      file: includes/base.yaml
      service: ${MM_INIT}
