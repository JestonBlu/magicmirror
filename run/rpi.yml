include:
  - includes/${MM_INIT}.yaml
  - includes/${MM_MMPM}.yaml

services:
  magicmirror:
    container_name: mm
    restart: unless-stopped
    extends:
      file: includes/base.yaml
      service: rpi_${MM_INIT}
